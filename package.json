{
  "name": "claude-tdd",
  "private": true,
  "type": "module",
  "description": "Dev tooling for the claude-tdd plugin's hooks — type checking only, no build step.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "smoke": "node e2e/smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "@types/node": "^22.20.1"
  }
}
